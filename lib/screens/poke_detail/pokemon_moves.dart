import 'package:flutter/material.dart';

class PokemonMoves extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Movimientos del Pokemon'));
  }
}
